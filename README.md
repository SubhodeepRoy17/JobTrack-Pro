# Job Application Tracker

A React-based application to track job applications with authentication, CRUD operations, search, filter, sort, and pagination features.

## Features

### ✅ Completed Features
1. **Authentication System**
   - Login with email validation
   - Role-based access (manager/user)
   - Protected routes
   - Persistent login session

2. **Job Application Management**
   - Add new applications with form validation
   - View all applications in a table
   - Edit existing applications
   - Delete applications

3. **Table Features**
   - Search by company name or job title
   - Filter by job type and status
   - Sort by company name or applied date
   - Pagination (5 items per page)

4. **Dashboard**
   - Summary statistics
   - Recent applications list

5. **Responsive Design**
   - Mobile-friendly navigation
   - Clean, modern UI

### 🚀 Bonus Features Implemented
- Inline editing for applications
- Delete confirmation
- Success/error messages
- Form validation with inline errors
- Persistent data (simulated with context)

## Tech Stack

- **React** (v18.2.0)
- **React Router DOM** (v6.8.0) for routing
- **Context API** for state management
- **CSS3** for styling

## Installation & Setup

1. Clone the repository:
```bash
git clone <repository-url>
cd job-application-tracker